IRC
^^^
Uses a simple socket connection to log in to an IRC server and verify a welcome
message is returned.

`Uses Accounts`

Custom Properties:

.. list-table::
   :widths: 25 50

   * - timeout
     - Connection timeout in seconds
   * - realname
     - Realname to send in the USER command
